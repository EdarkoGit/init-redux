const initialState = {
  fruta: "",
};

export const reducerTwo = (state = initialState, { type, payload }) => {
  switch (type) {
    /*     case value:
      
      break;
   */
    default:
      return state;
  }
};
