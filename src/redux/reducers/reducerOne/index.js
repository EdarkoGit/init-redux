const initialState = {
  edarcode: "",
};

export const reducerOne = (state = initialState, { type, payload }) => {
  switch (type) {
    /*     case value:
      
      return;
   */
    default:
      return state;
  }
};
